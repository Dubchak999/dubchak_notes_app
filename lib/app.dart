import 'package:dubchak_notes_app/feature/notes/pages/notes_page/notes_page.dart';
import 'package:flutter/material.dart';

class NotesApp extends MaterialApp {
  const NotesApp({Key? key}) : super(key: key, home: const NotesPage());
}
